<h1><%= t('.edit', body: @question.body) %></h1>

<p><%= @question.body %></p>

<h2><%= t('.answers') %></h2>

<%= link_to t('.new.header'), new_admin_question_answer_path(@question) %>

<ol><%= render @question.answers %></ol>

<%= link_to t('.shared.links.edit'), edit_admin_question_path(@question) %>
<%= link_to t('.shared.links.back'), admin_test_path(@question.test) %>
